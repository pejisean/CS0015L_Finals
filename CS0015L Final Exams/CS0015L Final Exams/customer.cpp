#include "customer.h"

// Constructor to initialize Customer
Customer::Customer(int id = 0, const string& name = "", const string& address = "") {
    Customer_ID = id;
    Name = name;
    Address = address;
};